<odoo>
    <record id="view_sahal_import_tree" model="ir.ui.view">
        <field name="name">sahal.import.tree</field>
        <field name="model">sahal.import</field>
        <field name="arch" type="xml">
            <tree>
                <field name="transfer_id"/>
                <field name="transaction_id"/>
                <field name="transfer_date"/>
                <field name="description"/>
                <field name="other_party_account"/>
                <!-- <field name="debit"/> -->
                <field name="credit"/>
                <field name="status"/>
            </tree>
        </field>
    </record>

    <record id="view_sahal_import_form" model="ir.ui.view">
        <field name="name">sahal.import.form</field>
        <field name="model">sahal.import</field>
        <field name="arch" type="xml">
            <form>
             <header>
                <field name="status" widget="statusbar" clickable="1"/>
                <button name="action_complete" type="object" string="Confirm" class="btn-primary" invisible="status == 'completed'"/>
                <button name="action_cancel" type="object" string="Cancel" class="btn-danger" invisible="status == 'cancelled' or status == 'completed'"/>
                <button name="action_reset_to_pending" type="object" string="Reset to Pending" class="btn-warning" invisible="status == 'pending'"/>
            </header>
                <sheet>
                <div class="oe_button_box" name="button_box">
                        <button type="object"
                                name="action_view_payments"
                                class="oe_stat_button"
                                icon="fa-money"
                               >
                            <field string="Payments" name="payment_ids" widget="statinfo"/>
                        </button>
                       
           <button type="object"
        name="action_view_previous_mobile_payments"
        class="oe_stat_button"
        icon="fa-phone"
        >
   
    <field name="previous_mobile_payment_count" widget="statinfo"/>
</button>

                    </div>
                    <group>
                        <field name="transfer_id"/>
                        <field name="customer_id" modifiers="{'readonly': [('status', '=', 'completed')]}" invisible="split_payment == True" style="color: #1976d2; font-weight: bold;"/>
                        <field name="transfer_date"/>
                        <field name="description"/>
                        <field name="other_party_account"/>
                        
                        <field name="credit"/>
                        <!-- <field name="balance"/>
                        <field name="status"/> -->
                        <field name="sender_mobile"/>
                         <field name="split_payment"/>
                    </group>
                  
                    <notebook invisible="split_payment == False" >
                        <page string="Payment Lines">
                            <field name="payment_line_ids">
                                <tree editable="bottom">
                                    <field name="partner_id"/>
                                    <field name="amount"/>
                                </tree>
                            </field>
                        </page>
                        </notebook>
                       
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="activity_ids" widget="mail_activity"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

     <record id="view_sahal_import_search" model="ir.ui.view">
        <field name="name">sahal.import.search</field>
        <field name="model">sahal.import</field>
        <field name="arch" type="xml">
            <search>
            <filter string="Pending" name="pending" domain="[('status','=','pending')]" />
            <filter string="Completed" name="completed" domain="[('status','=','completed')]"/>
            <filter string="Cancelled" name="cancelled" domain="[('status','=','cancelled')]"/>
            <filter name="group_by_status" string="Group by Status" context="{'group_by': 'status'}"/>
            <filter name="group_by_sender" string="Group by Sender" context="{'group_by': 'sender_mobile'}"/>
                 
            </search>
        </field>
    </record>

    <record id="action_sahal_import" model="ir.actions.act_window">
        <field name="name">Sahal Imports</field>
        <field name="res_model">sahal.import</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_pending': 1}</field>
    </record>

    <menuitem id="menu_sahal_import" name="Sahal Imports" parent="e_payment_menu_root" action="action_sahal_import"/>
</odoo>